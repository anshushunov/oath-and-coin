<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!--
    Consumes ScenarioOutcome's shape only — never the file system, never
    Godot. PresentationBoundaryTests (tests\OathAndCoin.Presentation.Tests)
    guards this stays true of the compiled assembly, not just of intent.
  -->
  <ItemGroup>
    <ProjectReference Include="..\OathAndCoin.Content\OathAndCoin.Content.csproj" />
  </ItemGroup>

</Project>
