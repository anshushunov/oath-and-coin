{
  "name": "@oath-and-coin/presentation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Read models and localization projections — the layer that turns a decision into something a screen can draw, and never resolves a key into text itself (ADR-010, TDD §11.1)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --root ../.. packages/presentation"
  },
  "dependencies": {
    "@oath-and-coin/simulation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
