{
  "name": "@oath-and-coin/simulation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure deterministic rules, identity and state transitions — imports nothing outside itself (ADR-002, ADR-010)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --root ../.. packages/simulation"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
