/**
 * Шапка сгенерированного `apps/web/src/ui/tokens.css`, объявленная один раз.
 *
 * Отдельный модуль, а не строка в каждом из двух скриптов, по той же причине, по которой
 * существует `tokens.ts` сам: шапка, написанная дважды, расходится. Генератор пишет свою
 * версию, проверка сравнивает со своей, и `pnpm tokens:check` начинает краснеть на пустом
 * месте — на пробеле в комментарии, а не на разошедшемся цвете. Красный, который ничего
 * не значит, дороже отсутствующей проверки: его учатся игнорировать.
 */
export const BANNER =
  '/* Сгенерировано `pnpm tokens:generate` из apps/web/src/ui/tokens.ts.\n' +
  '   Править здесь нечего: правка уедет при следующей генерации, а\n' +
  '   `pnpm tokens:check` покраснеет раньше этого. */\n\n';
