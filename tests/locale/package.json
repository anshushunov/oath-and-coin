{
  "name": "@oath-and-coin/locale-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The shipped locale catalogue held against every key the presentation layer can produce — the one check that needs to see both sides of the presentation/content boundary",
  "scripts": {
    "test": "vitest run --root ../.. tests/locale"
  },
  "devDependencies": {
    "@oath-and-coin/content": "workspace:*",
    "@oath-and-coin/presentation": "workspace:*",
    "@oath-and-coin/simulation": "workspace:*",
    "typescript": "6.0.3"
  }
}
