{
  "name": "@oath-and-coin/scenario-runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Headless scenario execution — the CLI that runs a scenario manifest through this port and reports its canonical hash (ADR-010; byte parity against the frozen corpus retired by ADR-013)",
  "main": "src/cli.ts",
  "types": "src/cli.ts",
  "scripts": {
    "test": "vitest run --root ../.. tools/scenario-runner"
  },
  "dependencies": {
    "@oath-and-coin/content": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
